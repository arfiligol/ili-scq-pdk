# Provider-Safe Notebooks

**Target:** `ili-scq-pdk`

**Status:** candidate

Public notebooks should exercise the same provider contract used by private
layout repos, while keeping private layout/IP outside public docs.

The public notebook surface should show activation, public cells, public
provider metadata, and workflow shape. Private resonator and qubit notebooks
remain in the private layout repo until a publication-safe surface exists.
