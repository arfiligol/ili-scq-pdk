# Reserved Models Namespace

`ili_scq_pdk.models` is reserved for future public model contracts.

This round intentionally does not implement analytical models, SAX/JAX models,
Palace wrappers, or simulation APIs. The active PDK scope is materials,
technology/layer stack, public cells, public samples, documentation, and the
private layout provider contract.
